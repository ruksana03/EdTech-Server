const createApplication = require('./createApplication')
const findAllApplication = require('./findAllApplication')
const findOneApplication = require('./findOneApplication')
const updatedStatusApplication = require('./updatedStatusApplication')
const rejectedApplication = require('./rejectedApplication')


module.exports = {
    createApplication,findAllApplication,findOneApplication,updatedStatusApplication,rejectedApplication
}

